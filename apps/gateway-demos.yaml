apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gateway-demos
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  generators:
    - list:
        elements:
          - cluster: k8s-lab
            server: https://kubernetes.default.svc
  template:
    metadata:
      name: gateway-demos-{{cluster}}
    spec:
      project: default
      source:
        repoURL: https://github.com/rayabueg/cluster-applications.git
        targetRevision: main
        path: apps-envs/gateway-demos
      destination:
        server: '{{server}}'
        namespace: envoy-gateway-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 3m
